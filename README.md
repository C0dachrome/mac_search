# mac_search

macSearch.py works in tandem with airodump-ng as a system to determine the MAC address of devices whose address is unknown.

automatedScan.py is a modified version of macSearch that is meant for my specific use case of mapping signal integrity at a movie theater. It uses a guided scanning process and averages scan values for a more accurate heatmap later on. 

